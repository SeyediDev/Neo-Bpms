@using Neo.Bpms.Domain.Entities.Cmmn.UI
@using Neo.Bpms.Domain.Entities.Cmmn.UI.Components
@using Neo.Bpms.Domain.Entities.Cmmn.UI.ConfiguredItems
@using Neo.Bpms.Domain.Entities.Cmmn.UI.Reports

@{
    var divConfiguration = ViewBag.DivConfiguration as ConfiguredDashboard.ConfigDiv;
    var widget = ViewBag.Widget as ConfiguredDashboard.ConfigWidget;
    var reportData = ViewBag.ReportData as ReportData;
    var maxRecord = ViewBag.MaxRecord ?? 15;
    var widgetHeight = ViewBag.WidgetHeight ?? 300;
}

@if (divConfiguration != null)
{
    <div class="modal fade cando-dashboard-modal" 
         id="widgetSettingsModal-@divConfiguration.WidgetId" 
         role="dialog" 
         tabindex="100">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">
                        <svg class="cando-dashboard-modal-icon" viewBox="0 0 24 24">
                            <use xlink:href="/Content/common-assets-includes/icons/svgSprite.svg#setting-dashboard" />
                        </svg>
                        تنظیمات ویجت
                    </h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                
                <div class="modal-body">
                    @if (reportData?.structure?.ReportViewType == ReportViewType.Chart)
                    {
                        <div class="cando-dashboard-form-group">
                            <label for="selectChart-@divConfiguration.WidgetId" class="cando-dashboard-form-label">
                                نوع نمودار
                            </label>
                            <select id="selectChart-@divConfiguration.WidgetId" class="cando-dashboard-form-input">
                                @foreach (Report.ChartType chartType in Enum.GetValues<Report.ChartType>())
                                {
                                    <option value="@chartType" selected="@(reportData.structure.ChartType == chartType)">
                                        @chartType
                                    </option>
                                }
                            </select>
                        </div>
                    }
                    
                    @* Records Count *@
                    <div class="cando-dashboard-form-group">
                        <label for="recordsCount-@divConfiguration.WidgetId" class="cando-dashboard-form-label">
                            تعداد رکوردها
                        </label>
                        <input type="number" 
                               class="cando-dashboard-form-input" 
                               id="recordsCount-@divConfiguration.WidgetId" 
                               value="@maxRecord"
                               min="1"
                               max="10000">
                    </div>
                    
                    @* Widget Dimensions *@
                    <div class="cando-dashboard-form-row">
                        <div class="cando-dashboard-form-group">
                            <label for="widgetWidth-@divConfiguration.WidgetId" class="cando-dashboard-form-label">
                                عرض ویجت
                            </label>
                            <input type="number" 
                                   min="1" 
                                   max="12" 
                                   id="widgetWidth-@divConfiguration.WidgetId" 
                                   value="@divConfiguration.Width" 
                                   class="cando-dashboard-form-input">
                        </div>
                        
                        <div class="cando-dashboard-form-group">
                            <label for="widgetHeight-@divConfiguration.WidgetId" class="cando-dashboard-form-label">
                                ارتفاع ویجت
                            </label>
                            <input type="number" 
                                   id="widgetHeight-@divConfiguration.WidgetId" 
                                   class="cando-dashboard-form-input"
                                   value="@widgetHeight"
                                   min="200"
                                   max="1000">
                        </div>
                    </div>
                </div>
                
                <div class="modal-footer">
                    <button type="button" 
                            class="cando-dashboard-btn cando-dashboard-btn-secondary" 
                            data-dismiss="modal">
                        لغو
                    </button>
                    <button type="button" 
                            class="cando-dashboard-btn cando-dashboard-btn-primary" 
                            widgetId="@divConfiguration.WidgetId"
                            onclick="dashboard_submitWidgetSettings(this, event)">
                        تایید
                    </button>
                </div>
            </div>
        </div>
    </div>
}

