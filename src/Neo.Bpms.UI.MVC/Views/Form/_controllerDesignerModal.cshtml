@using Neo.Bpms.UI.Resources.Resources
<div class="modal fade" id="controllerDesignerModal" tabindex="-1" role="dialog" aria-labelledby="controllerDesignerModal-label">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content  ">
            <div class="modal-header">
	            <h4 class="modal-title" id="controllerDesignerModal-label">@ViewTexts.ControllerDesign</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="@ViewTexts.Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="controllerDesignerApp"></div>
            </div>
        </div>
    </div>
</div>

    .modal-dialog { overflow-y: initial !important }

    .modal-body {
        max-height: calc(100vh - 200px);
        overflow-y: auto;
