/*namespace Neo.Bpms.Domain.Model.Data.Email
{
	using Entities;
	using ExternalDatabase;

	public class ExchangeDataSourceDefinition: OLEDBDataSourceDefinition
	{
		public ExchangeDataSourceDefinition(OledbConnectionDefinition connection, string name, Entity entity)
			: base(connection, name, entity)
		{
		}
	}
}*/